<Window x:Class="BEWebshop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - BEWebshop" Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="BEWebshop Login" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <TextBlock Grid.Row="1" Text="Email:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="EmailTextBox" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="Password:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="4" x:Name="PasswordBox" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="5" Orientation="Vertical">
            <Button Content="Login" Click="LoginButton_Click" 
                    Height="35" Margin="0,0,0,10" 
                    Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Content="Register" Click="RegisterButton_Click" 
                    Height="35" Background="#2196F3" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>