<UserControl x:Class="BEWebshop.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Orders" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Filter Section -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Filter by Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" SelectedItem="{Binding StatusFilter}" Height="30" Margin="0,0,10,0">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Pending"/>
                <ComboBoxItem Content="Processing"/>
                <ComboBoxItem Content="Shipped"/>
                <ComboBoxItem Content="Delivered"/>
                <ComboBoxItem Content="Cancelled"/>
            </ComboBox>
            <Button Grid.Column="2" Content="Apply Filter" Command="{Binding FilterByStatusCommand}" 
                    Width="100" Height="30"/>
        </Grid>

        <!-- Orders DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Orders}" 
                  SelectedItem="{Binding SelectedOrder}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Customer" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding CustomerEmail}" Width="200"/>
                <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=€{0:F2}}" Width="100"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="View Details" Command="{Binding DataContext.ViewOrderDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        Width="90" Margin="2" Padding="5"/>
                                <Button Content="Cancel" Command="{Binding DataContext.CancelOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        Width="60" Margin="2" Padding="5"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Order Details Section -->
        <Border Grid.Row="3" BorderBrush="#CCCCCC" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Order Management" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <TextBlock Text="Change Status to:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Button Content="Processing" Command="{Binding UpdateOrderStatusCommand}" 
                            CommandParameter="Processing" Width="90" Height="30" Margin="5"/>
                    <Button Content="Shipped" Command="{Binding UpdateOrderStatusCommand}" 
                            CommandParameter="Shipped" Width="80" Height="30" Margin="5"/>
                    <Button Content="Delivered" Command="{Binding UpdateOrderStatusCommand}" 
                            CommandParameter="Delivered" Width="80" Height="30" Margin="5"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
