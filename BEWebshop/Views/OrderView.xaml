<UserControl x:Class="BEWebshop.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Categories" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Categories DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Categories}" 
                  SelectedItem="{Binding SelectedCategory}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Product Count" Binding="{Binding Products.Count}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add Category" Command="{Binding AddCategoryCommand}" 
                    Width="120" Height="35" Margin="5"/>
            <Button Content="Edit Category" Command="{Binding EditCategoryCommand}" 
                    Width="120" Height="35" Margin="5"/>
            <Button Content="Delete Category" Command="{Binding DeleteCategoryCommand}" 
                    Width="120" Height="35" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
